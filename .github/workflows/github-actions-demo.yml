name: Build and deploy
on: push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
      
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.0.0
      with:
        server: access958372477.webspace-data.io
        username: u111854262
        password: ${{ secrets.ftp_password }}
        protocol: sftp
        port: 22
